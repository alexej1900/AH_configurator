import styles from './contactBtn.module.scss';

export default function ContactBtn() {

  return (
    <div className={`${styles.btn__getContacts} center`}>
        <h4>Kontakt</h4> 
        <h5>aufnehmen</h5>
    </div>
  )
}
